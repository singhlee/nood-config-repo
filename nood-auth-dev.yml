info:
  name: nood-auth
  author: singh
  description: 认证服务
server:
  port: 8764
  tomcat:
    uri-encoding: UTF-8

logging:
  path: logs
  level:
    root: INFO

eureka:
  instance:
    prefer-ip-address: true
    ip-address: ${spring.cloud.client.ip-address}:${server.port}
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone : http://${spring.security.user.name}:${spring.security.user.password}@localhost:8761/eureka/

spring:
  datasource:
    url: jdbc:mysql://148.70.23.139:3306/test?useUnicode=true&characterEncoding=UTF-8&zeroDateTimeBehavior=convertToNull&allowMultiQueries=true&useSSL=false
    username: mysql
    password: 67b81d29d3
    driver-class-name: com.mysql.cj.jdbc.Driver
  #spring.datasource.type
  # JPA配置
  jpa:
    hibernate:
      ddl-auto: update
    # 输出日志
    show-sql: true
    # 数据库类型
    database: mysql
# JWT Config
jwt:
  header: Authorization
  secret: NDU0NTY4amhmc3NkeHp6eGNxdzIlMjFAJTIxQCUyM2ZmNQ==
  expiration: 7200000